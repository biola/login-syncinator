login:
  db_connection_string: # set in settings.local.yml

redis:
  url: "redis://localhost:6379/0"

# See http://rdoc.info/github/mikel/mail/Mail#defaults-class_method
email:
  delivery_method: test
  options: {}

exception_notification:
  options:
    email_prefix: "[login-syncinator] "
    sender_address: "no-reply@biola.edu"
    exception_recipients:
      - appdev@biola.edu

trogdir:
  scheme: http
  host: trogdir-api.dev
  port:
  script_name:
  version: v1
  access_id: # set in settings.local.yml
  secret_key: # set in settings.local.yml
  api_timeout: 300 # 5 min
